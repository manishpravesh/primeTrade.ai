# PrimeTrade Frontend

## Setup

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start the UI:

   ```bash
   npm run dev
   ```

The app expects the backend at `http://localhost:4000` by default.
Override with `VITE_API_BASE` if needed.
