# PrimeTrade Backend

## Setup

1. Copy `.env.example` to `.env` and update the values.
2. Install dependencies:

   ```bash
   npm install
   ```

3. Start the API:

   ```bash
   npm run dev
   ```

## Health Check

`GET /health`
